<section class="wip-section" data-toggle="header" data-toggle-trigger="scroll">
    <div class="container document-page">
        {% include "./aside.nunj" %}
        <div class="main-content col-lg-10 col-md-8 col-sm-7">
            {% for feature in features %}
                <p>{{ feature.title }}</p>
            {% endfor %}
        </div>
    </div>
</section>